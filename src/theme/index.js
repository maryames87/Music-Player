import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  direction: "rtl",
  typography: {
    textAlign: "right",
    fontFamily: "IRANsansDN",
  },
  palette: {
    primary: {
      main: "#f50",
    },
  },
});
